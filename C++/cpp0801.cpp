#include <bits/stdc++.h>
#define ll long long
using namespace std;
int main() {
    freopen("PTIT.in", "r", stdin);
    freopen("PTIT.out", "w", stdout);
    string s;
    while (getline(cin, s))
        cout << s << endl;
}